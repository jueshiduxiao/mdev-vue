let routeList = [
    {
        path: '/',
        components: [ ]
    }
];
let rootWrapper = document.body;

let get = function () {
};

let getRoute = function () {
};

let on = function () {
};

let off = function () {
};

let pushState = function () {
};

let config = function (params) {
    params = params || {};
    routeList = params.routeList;
    rootWrapper = params.wrapper;
};

let run = function () {
    
};

export {
    config,
    run
};
