let modulePrototype = {
    get: () => {
        // return obj
    },
    set: (key, val) => {
        // return this
    },
    on: (key, handle) => {
        // return undefined
    },
    off: (key, handle) = > {
        // return undefined
    }
};

let module = {
    getModule: (moduleName) => {
        return // promise
    }
};

export default module;
